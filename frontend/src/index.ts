'use strict';
import "./css/style.css";
import "./scss/style.scss";
import Logo from './assets/img/howto.png';


/* import Icon from './assets/img/logo512.png';

import Print from "./print"; 

function component():HTMLElement {
    const element = <HTMLDivElement>document.createElement('div');
    const button = <HTMLButtonElement>document.createElement('button');

    // Lodash, currently included via a script, is required for this line to work
    element.textContent = 'Hello webpack' as string;
    button.textContent = 'Click me and check the console' as string;

    button.addEventListener('click', Print.bind(null, 'Hello Webpack!'));

    const myIcon = <HTMLImageElement>new Image();
    myIcon.src = Icon as string;

    element.append(myIcon);
    element.append(button);
    
    return element;
}

document.body.append(component()); */